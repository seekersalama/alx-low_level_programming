/**
* _pow_recursion - check the code
*@x: int
*@y: int
* Return: Always 0.
*/

int is_prime_number(int n)
{
	int i;

	i = 1;
	if(n % i || i = )
		i = 1;
	if(n % i == i)
		return (i);
	is_prime_number(n)
}
