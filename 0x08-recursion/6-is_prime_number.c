/**
 * _pow_recursion - check the code
 *@x: int
 *@y: int
 * Return: Always 0.
 */

int is_prime_number(int n)
{
	if( primenunmber(n, n/2))

	if(n % i || i = )
		i = 1;
	if(n % i == i)
		return (i);
	is_prime_number(n)
}



int primeno(int num)
{
int i;
i = n / 2;
	if (i == 1)
	{
		return i;
	}
	else
	{
		if (num % i == 0)
		{
			return 0;
		}
		else
		{
			return primeno(num, i - 1);
		}
	}
}
