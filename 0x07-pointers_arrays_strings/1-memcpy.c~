/**
 * _memcpy - prints buffer in hexa
 * @dest: char *the address of memory to print
 * @src: char the size of the memory to print
 *@n: unsigned int
 * Return: char *q
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i += 1)
	{
		*(dest + i) = *(src + i);
	}
	return (dest);
}
