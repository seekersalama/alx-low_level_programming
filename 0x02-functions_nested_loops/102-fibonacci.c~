#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	long int a, b, accu;
	int m;

	accu = 0;
	a = 0;
	b = 1;
	for (m = 1; m < 32 ; m++)
	{
		accu = a + b;
		printf("%ld, ", accu);
		a = b;
		b = accu;
	}

	printf("%ld\n", a + b);
	return (0);
}
