#include <stdio.h>

/**
 * main - Entry point
 * @c: input char to _putrchar
 *
 * Return: Always 0 (Success)
 */
int _putchar(char c);

int main(void)
{
	int i;
	char *cin = "_putchar\n";

	for (i = 0; cin[i] != '\n'; i++)
	{
		_putchar(cin[i]);
	}
	_putchar('\n');
	return (0);
}
