#include <stdio.h>

/**
 * main - Entry point if okay
 *
 * Return: Always 0 (Success) for all return
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
